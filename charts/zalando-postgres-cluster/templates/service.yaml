apiVersion: v1
kind: Service
metadata:
  name: {{ include "zalando-postgres-cluster.fullname" . }}
  labels:
    {{- include "zalando-postgres-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zalando-postgres-cluster.selectorLabels" . | nindent 4 }}
