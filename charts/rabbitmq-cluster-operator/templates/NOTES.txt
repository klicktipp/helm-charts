RabbitMQ Cluster Operator has been deployed into namespace `{{ include "rabbitmq-cluster-operator.namespace" . }}`.

Status:
  kubectl get pods -n {{ include "rabbitmq-cluster-operator.namespace" . }} -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get deploy -n {{ include "rabbitmq-cluster-operator.namespace" . }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.clusterOperator.watchAllNamespaces }}
Cluster Operator scope:
  all namespaces
{{- else }}
Cluster Operator scope:
{{- $namespaces := .Values.clusterOperator.watchNamespaces | default (list (include "rabbitmq-cluster-operator.namespace" .)) }}
{{- range $namespace := $namespaces }}
  - {{ $namespace }}
{{- end }}
{{- end }}

{{- if .Values.msgTopologyOperator.enabled }}

Messaging Topology Operator:
  managed through the `msgTopologyOperator` dependency
{{- if .Values.msgTopologyOperator.watchAllNamespaces }}
  scope: all namespaces
{{- else }}
  scope:
{{- $namespaces := .Values.msgTopologyOperator.watchNamespaces | default (list (include "rabbitmq-cluster-operator.namespace" .)) }}
{{- range $namespace := $namespaces }}
    - {{ $namespace }}
{{- end }}
{{- end }}

Webhook certificates:
{{- if .Values.msgTopologyOperator.useCertManager }}
  managed by cert-manager
{{- else if .Values.msgTopologyOperator.existingWebhookCertSecret }}
  existing secret: {{ .Values.msgTopologyOperator.existingWebhookCertSecret }}
{{- else }}
  generated by the `rabbitmq-topology-operator` subchart
{{- end }}

{{- if not .Values.msgTopologyOperator.skipCreateAdmissionWebhookConfig }}
Admission webhook:
  enabled
{{- else }}
Admission webhook:
  skipped via `msgTopologyOperator.skipCreateAdmissionWebhookConfig=true`
{{- end }}
{{- end }}

{{- if .Values.monitoring.service.enabled }}

Cluster Operator metrics service:
  kubectl get svc -n {{ include "rabbitmq-cluster-operator.namespace" . }} {{ include "rmqco.clusterOperator.fullname" . }}-metrics
{{- end }}

{{- if and .Values.msgTopologyOperator.enabled .Values.msgTopologyOperator.monitoring.service.enabled }}

Messaging Topology Operator metrics service:
  kubectl get svc -n {{ include "rabbitmq-cluster-operator.namespace" . }} -l app.kubernetes.io/component=messaging-topology-operator,app.kubernetes.io/instance={{ .Release.Name }}
{{- end }}
