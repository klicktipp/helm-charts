{{- if not .Values.clusterOperator.webhook.skipCreateAdmissionWebhookConfig }}
{{/*
    If the user does not have cert-manager and is not providing a secret with the certificates, the chart needs to generate the secret
  */}}
{{- $secretName := printf "%s" (include "rmqco.clusterOperator.webhook.fullname" .) }}
{{- $ca := genCA "rmq-cluster-operator-ca" 365 }}
{{- $cert := genSignedCert (include "rmqco.clusterOperator.fullname" .) nil (list (printf "%s.%s.svc" (include "rmqco.clusterOperator.webhook.fullname" .) (include "rabbitmq-cluster-operator.namespace" .)) (printf "%s.%s.svc.%s" (include "rmqco.clusterOperator.webhook.fullname" .) (include "rabbitmq-cluster-operator.namespace" .) .Values.clusterDomain)) 365 $ca }}
{{- if and (not .Values.clusterOperator.webhook.useCertManager) (not .Values.clusterOperator.webhook.existingWebhookCertSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "rabbitmq-cluster-operator.namespace" . }}
  labels:
    {{- include "rmqco.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "rabbitmq-cluster-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ include "rabbitmq-cluster-operator.secrets.lookup" (dict "secret" $secretName "key" "tls.crt" "defaultValue" $cert.Cert "context" $) }}
  tls.key: {{ include "rabbitmq-cluster-operator.secrets.lookup" (dict "secret" $secretName "key" "tls.key" "defaultValue" $cert.Key "context" $) }}
  ca.crt: {{ include "rabbitmq-cluster-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" $ca.Cert "context" $) }}
{{- end }}
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  labels:
    {{- include "rmqco.labels" . | nindent 4 }}
  annotations:
    {{- if and (.Values.clusterOperator.webhook.useCertManager) (not .Values.clusterOperator.webhook.existingWebhookCertSecret) }}
    cert-manager.io/inject-ca-from: {{ printf "%s/%s" (include "rabbitmq-cluster-operator.namespace" .) ( include "rmqco.clusterOperator.webhook.secretName" . ) }}
    {{- else if and (.Values.clusterOperator.webhook.useCertManager) (.Values.clusterOperator.webhook.existingWebhookCertSecret) }}
    cert-manager.io/inject-ca-from-secret: {{ printf "%s/%s" (include "rabbitmq-cluster-operator.namespace" .) ( include "rmqco.clusterOperator.webhook.secretName" . ) }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "rabbitmq-cluster-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  name: {{ template "rmqco.clusterOperator.webhook.fullname" . }}-mutating
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.clusterOperator.webhook.useCertManager }}
      caBundle: {{ include "rabbitmq-cluster-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.clusterOperator.webhook.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqco.clusterOperator.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-cluster-operator.namespace" . }}
        path: /mutate-rabbitmq-com-v1beta1-rabbitmqcluster
        port: 443
    failurePolicy: Fail
    name: mrabbitmqcluster-v1beta1.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - rabbitmqclusters
    sideEffects: None
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  labels:
    {{- include "rmqco.labels" . | nindent 4 }}
  annotations:
    {{- if and (.Values.clusterOperator.webhook.useCertManager) (not .Values.clusterOperator.webhook.existingWebhookCertSecret) }}
    cert-manager.io/inject-ca-from: {{ printf "%s/%s" (include "rabbitmq-cluster-operator.namespace" .) ( include "rmqco.clusterOperator.webhook.secretName" . ) }}
    {{- else if and (.Values.clusterOperator.webhook.useCertManager) (.Values.clusterOperator.webhook.existingWebhookCertSecret) }}
    cert-manager.io/inject-ca-from-secret: {{ printf "%s/%s" (include "rabbitmq-cluster-operator.namespace" .) ( include "rmqco.clusterOperator.webhook.secretName" . ) }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "rabbitmq-cluster-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  name: {{ template "rmqco.clusterOperator.webhook.fullname" . }}-validating
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.clusterOperator.webhook.useCertManager }}
      caBundle: {{ include "rabbitmq-cluster-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.clusterOperator.webhook.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqco.clusterOperator.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-cluster-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-rabbitmqcluster
        port: 443
    failurePolicy: Fail
    name: vrabbitmqcluster-v1beta1.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - rabbitmqclusters
    sideEffects: None
{{- end }}
