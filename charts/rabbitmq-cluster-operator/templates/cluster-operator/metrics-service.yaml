{{- if .Values.monitoring.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "rmqco.labels" . | nindent 4 }}
    type: metrics
  name: {{ printf "%s-metrics" (include "rmqco.clusterOperator.fullname" .) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "rabbitmq-cluster-operator.namespace" . }}
  {{- if or .Values.commonAnnotations .Values.monitoring.service.annotations }}
  {{- $annotations := include "rabbitmq-cluster-operator.tplvalues.merge" ( dict "values" ( list .Values.monitoring.service.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "rabbitmq-cluster-operator.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.monitoring.service.type }}
  {{- if (or (eq .Values.monitoring.service.type "LoadBalancer") (eq .Values.monitoring.service.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.monitoring.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if .Values.monitoring.service.clusterIP }}
  clusterIP: {{ .Values.monitoring.service.clusterIP }}
  {{- end }}
  {{- if eq .Values.monitoring.service.type "LoadBalancer" }}
  loadBalancerSourceRanges: {{ .Values.monitoring.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if (and (eq .Values.monitoring.service.type "LoadBalancer") (not (empty .Values.monitoring.service.loadBalancerIP))) }}
  loadBalancerIP: {{ .Values.monitoring.service.loadBalancerIP }}
  {{- end }}
  {{- if .Values.monitoring.service.sessionAffinity }}
  sessionAffinity: {{ .Values.monitoring.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.monitoring.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "rabbitmq-cluster-operator.tplvalues.render" (dict "value" .Values.monitoring.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.monitoring.service.ports.http }}
      targetPort: http
      protocol: TCP
      {{- if (and (or (eq .Values.monitoring.service.type "NodePort") (eq .Values.monitoring.service.type "LoadBalancer")) (not (empty .Values.monitoring.service.nodePorts.http))) }}
      nodePort: {{ .Values.monitoring.service.nodePorts.http }}
      {{- else if eq .Values.monitoring.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.monitoring.service.extraPorts }}
    {{- include "rabbitmq-cluster-operator.tplvalues.render" (dict "value" .Values.monitoring.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "rabbitmq-cluster-operator.tplvalues.merge" ( dict "values" ( list .Values.clusterOperator.podLabels .Values.commonLabels ) "context" . ) }}
  selector:
    {{- include "rmqco.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: rabbitmq-operator
{{- end }}
