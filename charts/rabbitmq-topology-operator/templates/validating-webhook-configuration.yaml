{{- if and (eq .Values.enabled true) (not .Values.skipCreateAdmissionWebhookConfig) }}
{{/*
    If the user does not have cert-manager and is not providing a secret with the certificates, the chart needs to generate the secret
  */}}
{{- $secretName := printf "%s" (include "rmqto.webhook.fullname" .) }}
{{- $ca := genCA "rmq-msg-topology-ca" 365 }}
{{- $cert := genSignedCert (include "rmqto.fullname" .) nil (list (printf "%s.%s.svc" (include "rmqto.webhook.fullname" .) (include "rabbitmq-topology-operator.namespace" .)) (printf "%s.%s.svc.%s" (include "rmqto.webhook.fullname" .) (include "rabbitmq-topology-operator.namespace" .) .Values.clusterDomain)) 365 $ca }}
{{- if and (not .Values.useCertManager) (not .Values.existingWebhookCertSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
  labels:
    {{- include "rmqto.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "tls.crt" "defaultValue" $cert.Cert "context" $) }}
  tls.key: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "tls.key" "defaultValue" $cert.Key "context" $) }}
  ca.crt: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" $ca.Cert "context" $) }}
{{- end }}
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  labels:
    {{- include "rmqto.labels" . | nindent 4 }}
  annotations:
    {{- if and (.Values.useCertManager) (not .Values.existingWebhookCertSecret)}}
    cert-manager.io/inject-ca-from: {{ printf "%s/%s" (include "rabbitmq-topology-operator.namespace" .) ( include "rmqto.webhook.secretName" . ) }}
    {{- else if and (.Values.useCertManager) (.Values.existingWebhookCertSecret)}}
    cert-manager.io/inject-ca-from-secret: {{ printf "%s/%s" (include "rabbitmq-topology-operator.namespace" .) ( include "rmqto.webhook.secretName" . ) }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  name: {{ template "rmqto.webhook.fullname.namespace" . }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-binding
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vbinding.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - bindings
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-exchange
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vexchange.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - exchanges
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-federation
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vfederation.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - federations
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-operatorpolicy
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: voperatorpolicy.kb.io
    rules:
    - apiGroups:
      - rabbitmq.com
      apiVersions:
      - v1beta1
      operations:
      - CREATE
      - UPDATE
      resources:
      - operatorpolicies
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1alpha1-superstream
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vsuperstream.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - superstreams
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-permission
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vpermission.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - permissions
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-topicpermission
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vtopicpermission.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - topicpermissions
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-policy
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vpolicy.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - policies
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-queue
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vqueue.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - queues
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-schemareplication
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vschemareplication.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - schemareplications
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-shovel
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vshovel.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - shovels
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-user
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vuser.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - users
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if not .Values.useCertManager }}
      caBundle: {{ include "rabbitmq-topology-operator.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" (default $ca.Cert .Values.existingWebhookCertCABundle) "context" $) }}
      {{- end }}
      service:
        name: {{ template "rmqto.webhook.fullname" . }}
        namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
        path: /validate-rabbitmq-com-v1beta1-vhost
        port: {{ .Values.service.ports.webhook }}
    failurePolicy: Fail
    name: vvhost.kb.io
    rules:
      - apiGroups:
          - rabbitmq.com
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
        resources:
          - vhosts
    sideEffects: None
{{- end }}
