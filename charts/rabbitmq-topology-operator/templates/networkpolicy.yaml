{{- if .Values.networkPolicy.enabled }}
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ template "rmqto.fullname" . }}
  namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
  labels:
    {{- include "rmqto.labels" . | nindent 4}}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- $podLabels := include "rabbitmq-topology-operator.tplvalues.merge" ( dict "values" ( list .Values.podLabels .Values.commonLabels ) "context" . ) }}
  podSelector:
    matchLabels:
      {{- include "rmqto.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: messaging-topology-operator
      app.kubernetes.io/part-of: rabbitmq
  policyTypes:
    - Ingress
    - Egress
  {{- if .Values.networkPolicy.allowExternalEgress }}
  egress:
    - {}
  {{- else }}
  egress:
    - ports:
        # Allow dns resolution
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
        # Allow rabbitmq api
        - port: 15672
          protocol: TCP
        # Allow access to kube-apiserver
        {{- range $port := .Values.networkPolicy.kubeAPIServerPorts }}
        - port: {{ $port }}
        {{- end }}
    # RabbitMQCluster instances have the label app.kubernetes.io/component: rabbitmq
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: rabbitmq
        {{- if not .Values.watchAllNamespaces }}
        {{- $watchNamespaces := default (list (include "rabbitmq-topology-operator.namespace" .)) .Values.watchNamespaces }}
          namespaceSelector:
            matchExpressions:
              - key: namespace
                operator: In
                values:
                {{- range $namespace := $watchNamespaces }}
                  - {{ $namespace }}
                {{- end }}
        {{- end }}
    {{- if .Values.networkPolicy.extraEgress }}
    {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" .Values.networkPolicy.extraEgress "context" $ ) | nindent 4 }}
    {{- end }}
  {{- end }}
  ingress:
    - ports:
        {{/* Webhook port is hardcoded in the operator code */}}
        - port: 9443
        {{- if .Values.monitoring.service.enabled }}
        - port: {{ .Values.containerPorts.metrics }}
        {{- end }}
      {{- if not .Values.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{- include "rmqto.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/part-of: rabbitmq
        - podSelector:
            matchLabels:
              {{ template "rmqto.fullname" . }}-client: "true"
        {{- if .Values.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- range $key, $value := .Values.networkPolicy.ingressNSMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- if .Values.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- range $key, $value := .Values.networkPolicy.ingressNSPodMatchLabels }}
              {{ $key | quote }}: {{ $value | quote }}
              {{- end }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.networkPolicy.extraIngress }}
    {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" .Values.networkPolicy.extraIngress "context" $ ) | nindent 4 }}
    {{- end }}
{{- end }}
