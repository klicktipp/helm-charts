{{- if and .Values.enabled .Values.metrics.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "rmqto.labels" . | nindent 4 }}
    type: metrics
  name: {{ printf "%s-metrics" (include "rmqto.fullname" .) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "rabbitmq-topology-operator.namespace" . }}
  {{- if or .Values.commonAnnotations .Values.metrics.service.annotations }}
  {{- $annotations := include "rabbitmq-topology-operator.tplvalues.merge" ( dict "values" ( list .Values.metrics.service.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "rabbitmq-topology-operator.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.metrics.service.type }}
  {{- if (or (eq .Values.metrics.service.type "LoadBalancer") (eq .Values.metrics.service.type "NodePort")) }}
  externalTrafficPolicy: {{ .Values.metrics.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if .Values.metrics.service.clusterIP }}
  clusterIP: {{ .Values.metrics.service.clusterIP }}
  {{- end }}
  {{- if eq .Values.metrics.service.type "LoadBalancer" }}
  loadBalancerSourceRanges: {{ .Values.metrics.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if (and (eq .Values.metrics.service.type "LoadBalancer") (not (empty .Values.metrics.service.loadBalancerIP))) }}
  loadBalancerIP: {{ .Values.metrics.service.loadBalancerIP }}
  {{- end }}
  {{- if .Values.metrics.service.sessionAffinity }}
  sessionAffinity: {{ .Values.metrics.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.metrics.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "rabbitmq-topology-operator.tplvalues.render" (dict "value" .Values.metrics.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.metrics.service.ports.http }}
      targetPort: http-metrics
      protocol: TCP
      {{- if (and (or (eq .Values.metrics.service.type "NodePort") (eq .Values.metrics.service.type "LoadBalancer")) (not (empty .Values.metrics.service.nodePorts.http))) }}
      nodePort: {{ .Values.metrics.service.nodePorts.http }}
      {{- else if eq .Values.metrics.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.metrics.service.extraPorts }}
    {{- include "rabbitmq-topology-operator.tplvalues.render" (dict "value" .Values.metrics.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  selector:
    {{- include "rmqto.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: messaging-topology-operator
{{- end }}
